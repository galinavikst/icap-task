import { Typography } from "@mui/material";
import React from "react";

export default function AppTittle() {
  return (
    <Typography sx={{ fontSize: { xs: 30, md: 40, lg: 60 } }} variant="h1">
      All about Products
    </Typography>
  );
}
